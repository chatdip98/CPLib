#include <bits/stdc++.h>
#define ini(arr, val) memset(arr, val, sizeof(arr))
#define display(arr, n) for(i=0; i<n; i++) cout << arr[i] << " "; cout << endl;
#define sum(arr, n) accumulate(arr, arr+n, 0, plus<int>())
using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int n, i, k, t;	
	
	return 0;
}
